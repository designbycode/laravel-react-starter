<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Admin Routes
|--------------------------------------------------------------------------
|
| Define routes for admin-only areas here. You can apply role/permission
| middleware as needed. Example:
|
| Route::middleware(['auth', 'verified', 'role:Admin'])->prefix('admin')->as('admin.')->group(function () {
|     // Route::get('/', fn () => ...)->name('dashboard');
| });
|
*/
